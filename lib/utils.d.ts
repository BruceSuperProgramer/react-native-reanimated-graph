import { AXIS_DATA } from './config';
export declare const buildGraphPath: (axisValues: [AXIS_DATA]) => {
    path: import("react-native-redash").Path;
    pathString: string;
    minX: number;
    maxX: number;
    minY: number;
    maxY: number;
};
